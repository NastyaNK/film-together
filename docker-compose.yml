version: "3.1"

services:

  app:
    build:
      dockerfile: Dockerfile
    depends_on:
      - postgres

  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: anastasia
      POSTGRES_DB: vk
      POSTGRES_PASSWORD: 2553
    ports:
      - "5432:5432"